git clone https://github.com/ihando/datasci350-quiz01.git
cd datasci350-quiz01
touch commands.txt
git checkout -b feature-update
mkdir reports
touch reports/summary.md
echo "This document provides a summary of the project updates." >> reports/summary.md
git add reports
git commit -m "Add reports directory"
touch reports/file{1,2,3}.txt
mv data/raw-data.csv data/input-data.csv
mkdir scripts/backup && cp scripts/*.py scripts/backup/
rm docs/documentation.md
git add .
git commit -m "Update project files"
touch .gitignore
echo "temp/" >> .gitignore
echo "dataset0?.csv" >> .gitignore
cat .gitignore
git add .gitignore && git commit -m "Add .gitignore file"
git add commands.txt
git commit -m "Updated commands file so I can merge"
git checkout main && git merge feature-update
git add commands.txt && git commit -m "Documented all commands" && git push origin main

Bonus Questions:
1. I tried using gh but it got very confusing so I have switched to using github. 
